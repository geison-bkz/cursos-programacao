/**
    Array
    0 - Lista de Dados
    1 - Conjunto de Dados
 */

const arrayNumeros = [1, 2, 3, 4];
const arrayString = ['a', 'b', 'Geison'];
const array = [arrayNumeros, arrayString, true, { nome: 'Qualquer' }];

console.log(array);
